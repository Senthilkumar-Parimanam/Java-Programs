package A_Basics.LanguageStructure.DerievedDataType.Arrays;

import java.util.Arrays;
import java.util.Comparator;
import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.IntStream;

public class ArrayEx {
	
	public static void main(String[] arg) {
		
		//call(arg);
		
		//localArray(); 



		int[] intArray = {10,4,55,32,23};
		 //Arrays.stream(intArray).collect(Collectors.groupingBy(Collectors.groupingBy()));


        System.out.println(" the size of intArray :: "+ intArray.length);


		int[] a = new int[10];
		for(int i=0; i <a.length; i++)
			System.out.println(a[i]);


		//Comparator
	}
	

}
